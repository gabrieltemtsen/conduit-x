# Next.js console dashboard — server-side routes read these directly
# (no NEXT_PUBLIC_ prefix needed, they never reach the browser bundle).

BUYER_HEDERA_ACCOUNT_ID=0.0.5694300
HEDERA_HCS_TOPIC_ID=0.0.5694210
