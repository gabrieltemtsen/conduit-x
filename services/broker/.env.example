# Autonomous buyer agent (discover -> score -> pay -> failover).
# Payment settlement is delegated to @conduitx/x402-hedera — see that
# package's .env.example for the buyer credentials this service needs.

PORT=4005
STANDALONE=true
