# DEX Pool Intelligence seller (The Graph).
# Without GRAPH_API_KEY this falls back to the public (unauthenticated) endpoint.

PORT=4001
GRAPH_API_KEY=
SELLER_POOLS_ACCOUNT_ID=0.0.5694201
STANDALONE=true
